# Reviewer: Dependency

Review lockfile/manifest diff: new deps, licenses, CVE signals, unpinned versions.

Scope: PR dependency changes only.

Uses lens from [`patterns/dependency-review`](../patterns/dependency-review/dependency-review.md).
