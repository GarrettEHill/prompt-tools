# Onboarding Brief

Read-only. Produce brief using [`brief-template.md`](brief-template.md).

## Sections (required)

1. What this repo does (1 paragraph)
2. Layout — top-level dirs and purpose
3. Build — commands that work
4. Test — commands that work
5. Run / deploy — local dev path
6. Debug — logs, common failures
7. Key docs — links in reading order
8. Where to start for common tasks

## Validation

Spot-run documented commands when safe. Mark unverified commands explicitly.

## Output

Markdown brief only unless config says `write: docs/onboarding-brief.md`.
