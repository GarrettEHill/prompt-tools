# Mapper: Deploy Topology

**Class:** map

## You map

- compose/k8s/workflows, services, networks, ports
- build → deploy path

## Read-only checks

- `compose/`, `deploy/`, `.github/workflows/`, Dockerfiles
- `docker compose config` if available (read-only)

## Output

`deploy_surfaces` entries per map-result-schema.
