# Mapper: API Surface

**Class:** map

## You map

- public exports, routes, handlers, RPC/IPC entrypoints
- README/API doc claims vs visible surface

## Read-only checks

- scan route definitions, `pub fn`, OpenAPI, `app.get`, etc.
- list external vs internal modules

## Output

Module entries with `role: entrypoint`, `entrypoints` list per [`map-result-schema`](../../primitives/map-result-schema/map-result-schema.md).
