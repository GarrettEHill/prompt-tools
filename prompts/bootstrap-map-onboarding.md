# Remote map bootstrap — onboarding brief

```yaml
repo: <owner/repo>
output: chat-only | docs/onboarding-brief.md
use_prior_map: optional path or skip
```

Fetch manifest files. Execute [`patterns/onboarding-brief/onboarding-brief.md`]. Read-only unless `output` writes a file.
